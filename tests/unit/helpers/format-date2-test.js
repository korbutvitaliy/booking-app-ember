import { formatDate2 } from 'booking-app-ember/helpers/format-date2';
import { module, test } from 'qunit';

module('Unit | Helper | format date2');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatDate2([42]);
  assert.ok(result);
});
