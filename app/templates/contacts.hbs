{{#if isServiceProvider}}
	{{#each accepted as |booking|}}
		<div class="panel panel-default">
    	<div class="panel-heading">
      	<h3 class="panel-title">{{booking.bookedService.name}}</h3>
      </div>
      <div class="panel-heading">
        <h3 class="panel-title">{{booking.whoBooked.id}}</h3>
    	</div>
    </div>
	{{/each}}
{{/if}}
{{#if isConsumer}}
	{{#each accepted as |booking|}}
    <div class="panel panel-default">
      <div class="panel-heading">
          <h3 class="panel-title">{{booking.bookedService.name}}</h3>
      </div>
    </div>
	{{/each}}
{{/if}}